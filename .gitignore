# Secrets - never commit
.secrets
*.key
cosign.key
cosign.pub

# Corporate CA certs — keep only README in certs/
certs/*.crt
certs/*.pem

# Stage-level certs (copied by build scripts, cleaned up after build)
stage*/certs/

# Artifacts
artifacts/

# OS
.DS_Store
Thumbs.db
